"use client";

import Landing from "../components/homeluxe/landing/Landing";

/**
 * The front door.
 *
 * This used to be the showroom itself, which is a demo rather than a
 * business: somebody arriving was dropped inside a house with no idea what it
 * was, who ran it, or what it would cost a shop to be in it. The house has
 * moved to /showroom and this answers those three questions first.
 */
export default function Home() {
  return <Landing />;
}
